<h2><%= @post.title %></h2>
<%= link_to "edit", edit_post_path(@post) %>
<p>A <%= @post.category %> post.</p>
<hr />
<p><%= @post.content %></p>

<!-- <hr> tag defines a thematic change between paragraph-level
 elements in an HTML page. In previous versions of HTML,
 it was used to draw a horizontal line on the page visually
 separating the content. In HTML5 it has a semantic tag meaning.-->